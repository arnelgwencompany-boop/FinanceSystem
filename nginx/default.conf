server {
    listen 80;

    location / {
        root /app/build;
        index index.html;
        try_files $uri /index.html;
    }

    location /api/ {
        proxy_pass http://backend:8000;
    }
}